#include <bits/stdc++.h>
using namespace std;
int main(){
  vector <long> v(4);
  for(int i = 0; i<4;i++){
    cin>>v[i];
  }
  int sum=0;
  sort(v.begin(), v.end());
  for(int i = 0; i<3;i++){
    if(v[i] == v[i+1]){
      sum++;
    }
  }
  cout<<sum;
  return 0;
}