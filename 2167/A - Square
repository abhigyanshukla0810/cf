#include <stdio.h>
 
int main()
{
    int n;
    scanf("%d",&n);
    while(n--){
        int a,b,c,d;
        scanf("%d %d %d %d",&a,&b,&c,&d);
        if(a == b && a==c && a==d){
            printf("YES
");
        }
        else{
            printf("NO
");
        }
    }
 
    return 0;
}